[
  {kernel, [
    {logger, [
      {handler, default, logger_std_h, #{
        level => info, 
        config => #{file => "log/console.log"}
      }}
    ]}
  ]},
  {mnesia, [
    {dir, "./priv/mnesia"}
  ]},
  {registration_ws, [
    {port, 8080}
  ]}
].
